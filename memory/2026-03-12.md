# 2026-03-12

- Enabled OpenRouter in `/home/claw/.openclaw/openclaw.json` with `openrouter:default` auth profile and env key.
- Added model aliases for routing experiments:
  - `grok-legal` → `openrouter/x-ai/grok-3-beta`
  - `router-cheap` → `openrouter/openai/gpt-4.1-mini`
  - `router-verify` → `openrouter/anthropic/claude-3.5-sonnet`
- Smoke-tested live model switch in-session:
  - switched to `grok-legal` successfully
  - switched back to default Codex successfully
- User directive: make CLAW proactive/always-on, continuously improving, and explicitly self-recording (memory + operational docs), not passive.
- User requested focus on durable recording/integration now; Mission Control UI issue deferred.
- Started always-on execution framework:
  - Added `scripts/always_on_runner.sh` with ingest/research/validation/reporting loops.
  - Added `scripts/always_on_cron_install.sh` and installed cron block (`CLAW_ALWAYS_ON_START/END`).
  - Cron cadence installed:
    - every 30 min: full loop run
    - hourly at :15: reporting refresh
  - Initial full run executed successfully; outputs written to:
    - `reports/always_on/runs.log`
    - `reports/always_on/LATEST.md`
    - `state/always_on/status.json`
